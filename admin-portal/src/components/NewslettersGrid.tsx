import React from 'react';
import { ComingSoonPlaceholder } from './ComingSoonPlaceholder';

export const NewslettersGrid: React.FC = () => {
  return (
    <ComingSoonPlaceholder
      title="Newsletters"
      message="Newsletter management features are currently under development."
    />
  );
};

export default NewslettersGrid;

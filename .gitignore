# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Workspace package-lock.json files (workspaces use root lock file only)
# Lesson learned: November 15, 2025 - Individual workspace lock files cause OWASP Dependency Check failures
api/package-lock.json
admin-portal/package-lock.json
member-portal/package-lock.json
packages/*/package-lock.json

# Build outputs (ignored globally)
dist/
build/

# EXCEPTION: Track api-client dist files (needed for CI/CD)
# Lesson learned: November 14, 2025 - Missing dist files caused member portal build failure
# See: docs/LESSONS_LEARNED_BUILD_FAILURE_2025-11-14.md
!packages/api-client/dist/

# Azure
.azure-pipelines/variables.secret.yml

# Environment
.env
.env.local
.env.test
# .env.production - TRACKED in git for deployment (contains non-secret config)
.env.production.local
*.local

# Zitadel secrets (NEVER commit these)
.env.zitadel
zitadel-credentials.json

# Azure Functions local settings (contains secrets)
local.settings.json
api/local.settings.json

# Claude Code local settings (may contain sensitive data)
.claude/settings.local.json

# IDE
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
logs/
*.log

# Code review reports
**/reports/*.json
**/reports/*.txt
**/reports/*.html

# Keep report directory structure
!**/reports/.gitkeep
!**/reports/.gitignore

# Keep markdown summaries for visibility
!**/reports/*-summary.md
.credentials

# Playwright
playwright-report/
test-results/
.playwright/

# E2E Test Reports (may contain sensitive test data)
**/E2E_TEST_REPORT_*.md
**/E2E_TEST_SUMMARY.md

# Playwright Authentication State (contains session tokens)
playwright/.auth/
**/playwright/.auth/
*.auth.json
user.json

# Debug artifacts
**/debug/
diagnostic-*.png
diagnostic-*.jpg
screenshot-*.png
screenshot-*.jpg
*-dump.json
*-trace.json
*.har
console-logs.json
network-requests.json
debug-summary.txt

# Compiled TypeScript source maps (ignored globally)
*.js.map

# EXCEPTION: Track api-client source maps (needed for debugging)
!packages/api-client/dist/**/*.js.map

# Keep JavaScript config files
!*.config.js
!vite.config.js
!jest.config.js
!.eslintrc.js
*.backup
docs/MANTINE_LLMS.txt
# BDI RSA keys
api/keys/
api/scripts/keys/
api/tests/*.sh

# Code quality reports (generated)
complexity-report/
**/complexity-report/

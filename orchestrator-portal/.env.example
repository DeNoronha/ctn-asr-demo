# API Configuration (required)
# Backend API base URL for orchestrator operations
# Local development: http://localhost:3001/api/v1 (mock API)
# Production: Set via Azure DevOps pipeline
VITE_API_BASE_URL=http://localhost:3001/api/v1

# Note: The orchestrator portal uses a mock API for local development
# To start the mock API: npm run mock-api
# In production, this is set by the Azure DevOps pipeline to the actual Azure Functions endpoint

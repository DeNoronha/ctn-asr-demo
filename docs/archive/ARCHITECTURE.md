# System Architecture Diagram - Kendo React Integration

## 🏗️ Complete System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                                 │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                  AZURE STATIC WEB APPS                               │
│  URL: https://calm-tree-03352ba03.1.azurestaticapps.net            │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    REACT APPLICATION                                 │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │                     App.tsx (Main)                              ││
│  │  ┌──────────────┐ ┌────────────────────────────────────────┐  ││
│  │  │              │ │      DrawerContent                      │  ││
│  │  │  AdminSidebar│ │  ┌──────────────────────────────────┐  │  ││
│  │  │  (Kendo      │ │  │        Header                     │  │  ││
│  │  │   Drawer)    │ │  │  [☰] CTN Association Register    │  │  ││
│  │  │              │ │  └──────────────────────────────────┘  │  ││
│  │  │  📊Dashboard │ │                                          │  ││
│  │  │  👥Members   │ │  ┌──────────────────────────────────┐  │  ││
│  │  │  🔑Tokens    │◄─┼─►│      Content Area (Views)        │  │  ││
│  │  │  ⚙️Settings  │ │  │                                  │  │  ││
│  │  │  📚Docs      │ │  │  [Dashboard View]                │  │  ││
│  │  │              │ │  │  ┌────────┬────────┬────────┐   │  │  ││
│  │  └──────────────┘ │  │  │ Total  │ Active │Premium │   │  │  ││
│  │                    │  │  │   15   │   12   │   5    │   │  │  ││
│  │                    │  │  └────────┴────────┴────────┘   │  │  ││
│  │                    │  │                                  │  │  ││
│  │                    │  │  [Members View]                  │  │  ││
│  │                    │  │  ┌──────────────────────────┐   │  │  ││
│  │                    │  │  │   MembersGrid (Kendo)    │   │  │  ││
│  │                    │  │  │  - Search bar            │   │  │  ││
│  │                    │  │  │  - Sortable columns      │   │  │  ││
│  │                    │  │  │  - Pagination            │   │  │  ││
│  │                    │  │  │  - Action buttons        │   │  │  ││
│  │                    │  │  └──────────────────────────┘   │  │  ││
│  │                    │  │                                  │  │  ││
│  │                    │  │  [Tokens View]                   │  │  ││
│  │                    │  │  [Settings View]                 │  │  ││
│  │                    │  │  [Docs View]                     │  │  ││
│  │                    │  └──────────────────────────────────┘  │  ││
│  │                    └────────────────────────────────────────┘  ││
│  └────────────────────────────────────────────────────────────────┘│
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           │ HTTP Requests (Axios)
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        API SERVICE LAYER                             │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │                    api.ts (services/)                           ││
│  │                                                                  ││
│  │  export const api = {                                           ││
│  │    getMembers(): Promise<Member[]>                              ││
│  │    createMember(data): Promise<Member>                          ││
│  │    issueToken(orgId): Promise<TokenResponse>                    ││
│  │  }                                                               ││
│  │                                                                  ││
│  │  Base URL: process.env.REACT_APP_API_BASE_URL                  ││
│  └────────────────────────────────────────────────────────────────┘│
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           │ HTTPS
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                   BACKEND API (Azure App Service)                    │
│  URL: https://asr-api.azurewebsites.net/api                         │
│                                                                       │
│  Endpoints:                                                           │
│  • GET  /members          - List all members                         │
│  • POST /members          - Create new member                        │
│  • POST /tokens/issue     - Issue BVAD token                         │
└──────────────────────────┬──────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────────┐
│                          DATABASE                                     │
│  (Azure SQL / PostgreSQL / etc.)                                     │
│                                                                       │
│  Tables:                                                              │
│  • members (org_id, legal_name, domain, status, etc.)               │
│  • tokens (token_id, org_id, issued_at, expires_at)                 │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Data Flow Diagram

### User Action: Search for Members

```
┌──────┐
│ User │ Types "acme" in search box
└───┬──┘
    │
    ▼
┌─────────────────┐
│ MembersGrid     │ handleSearchChange()
│ Component       │
└───┬─────────────┘
    │
    │ setFilter({
    │   logic: 'or',
    │   filters: [
    │     { field: 'legal_name', operator: 'contains', value: 'acme' },
    │     { field: 'org_id', operator: 'contains', value: 'acme' },
    │     ...
    │   ]
    │ })
    │
    ▼
┌─────────────────┐
│ useEffect       │ Monitors filter changes
│ Hook            │
└───┬─────────────┘
    │
    │ let data = [...members];
    │ data = filterBy(data, filter);  ← Kendo Data Query
    │ setGridData(data);
    │
    ▼
┌─────────────────┐
│ Kendo Grid      │ Re-renders with filtered data
│ Component       │
└─────────────────┘
    │
    ▼
┌──────┐
│ User │ Sees filtered results instantly
└──────┘
```

### User Action: Issue Token

```
┌──────┐
│ User │ Clicks "Issue Token" for org:acme
└───┬──┘
    │
    ▼
┌─────────────────┐
│ MembersGrid     │ onClick={() => onIssueToken('org:acme')}
│ ActionCell      │
└───┬─────────────┘
    │
    ▼
┌─────────────────┐
│ App.tsx         │ handleIssueToken('org:acme')
│                 │
└───┬─────────────┘
    │
    │ api.issueToken('org:acme')
    │
    ▼
┌─────────────────┐
│ api.ts          │ POST /tokens/issue
│ Service         │
└───┬─────────────┘
    │
    │ HTTPS Request
    ▼
┌─────────────────┐
│ Backend API     │ Generates JWT token
│                 │
└───┬─────────────┘
    │
    │ Returns { access_token: "eyJ..." }
    ▼
┌─────────────────┐
│ App.tsx         │ setToken(response.access_token)
│                 │ setSelectedView('tokens')
└───┬─────────────┘
    │
    ▼
┌─────────────────┐
│ Token View      │ Displays token in textarea
│                 │
└─────────────────┘
    │
    ▼
┌──────┐
│ User │ Sees token and can copy it
└──────┘
```

---

## 📦 Component Structure

```
src/
├── App.tsx (Main Application)
│   ├── State Management
│   │   ├── members: Member[]
│   │   ├── loading: boolean
│   │   ├── token: string
│   │   ├── drawerExpanded: boolean
│   │   └── selectedView: string
│   │
│   ├── Effects
│   │   └── useEffect(() => loadMembers(), [])
│   │
│   ├── Handlers
│   │   ├── loadMembers()
│   │   ├── handleSubmit()
│   │   ├── handleIssueToken()
│   │   └── handleMenuSelect()
│   │
│   └── Render
│       ├── AdminSidebar
│       └── DrawerContent
│           ├── Header
│           └── Content (view-based)
│
├── components/
│   ├── AdminSidebar.tsx
│   │   ├── Props: expanded, onSelect, selectedItem
│   │   ├── State: None (stateless)
│   │   └── Kendo Drawer with menu items
│   │
│   └── MembersGrid.tsx
│       ├── Props: members, onIssueToken, loading
│       ├── State: gridData, sort, filter, searchValue
│       ├── Effects: useEffect(filter/sort data)
│       ├── Custom Cells:
│       │   ├── StatusCell (colored badges)
│       │   ├── MembershipCell (colored badges)
│       │   ├── DateCell (formatted date)
│       │   └── ActionCell (issue token button)
│       └── Kendo Grid with toolbar
│
└── services/
    └── api.ts
        ├── Configuration: Axios instance
        ├── Types: Member, TokenResponse
        └── Methods:
            ├── getMembers()
            ├── createMember()
            └── issueToken()
```

---

## 🎨 Styling Architecture

```
Styles/
├── Global Styles (index.css)
│   ├── CSS Reset
│   ├── Base typography
│   └── Body styles
│
├── Kendo Theme (imported in App.tsx)
│   ├── @progress/kendo-theme-default/dist/all.css
│   ├── Grid styles
│   ├── Drawer styles
│   ├── Button styles
│   └── Input styles
│
├── App.css (Layout & Theme)
│   ├── App container (flexbox)
│   ├── Header styles
│   ├── Content area
│   ├── View-specific styles
│   │   ├── Dashboard (stats grid)
│   │   ├── Members (form)
│   │   ├── Tokens (display)
│   │   └── Docs (content)
│   ├── Form components
│   ├── Responsive breakpoints
│   └── Kendo overrides
│
├── AdminSidebar.css
│   ├── Drawer container
│   ├── Drawer items
│   ├── Active state
│   ├── Hover effects
│   └── Mini mode
│
└── MembersGrid.css
    ├── Grid container
    ├── Toolbar styles
    ├── Badge styles
    ├── Grid overrides
    └── Responsive adjustments
```

---

## 🔌 External Dependencies

```
Dependencies Tree:

react (19.2.0)
├── react-dom (19.2.0)
└── react-scripts (5.0.1)
    ├── webpack
    ├── babel
    └── typescript (4.9.5)

@progress/kendo-react-layout (8.4.0)
└── Drawer component

@progress/kendo-react-grid (8.4.0)
├── Grid component
└── GridColumn component

@progress/kendo-data-query (1.6.0)
├── filterBy()
└── orderBy()

@progress/kendo-react-buttons (8.4.0)
└── Button component

@progress/kendo-react-inputs (8.4.0)
└── Input component

@progress/kendo-theme-default (9.3.0)
└── CSS theme files

axios (1.12.2)
└── HTTP client for API calls
```

---

## 🚀 Build & Deploy Pipeline

```
┌──────────────┐
│ Developer    │ Makes changes to code
└──────┬───────┘
       │
       │ npm run build
       ▼
┌──────────────┐
│ React Scripts│ Builds optimized bundle
└──────┬───────┘
       │
       │ Creates build/ folder
       │ - HTML (index.html)
       │ - JS (main.*.js, chunk.*.js)
       │ - CSS (main.*.css)
       │ - Assets (images, fonts)
       │
       ▼
┌──────────────┐
│ Git          │ git add . && git commit && git push
└──────┬───────┘
       │
       │ Pushes to GitHub
       ▼
┌──────────────┐
│ GitHub       │ Triggers workflow
│ Actions      │
└──────┬───────┘
       │
       │ Runs: npm install && npm run build
       ▼
┌──────────────┐
│ Azure        │ Deploys to Static Web Apps
│ Static Web   │
│ Apps         │
└──────┬───────┘
       │
       │ Uploads to CDN
       ▼
┌──────────────┐
│ Production   │ https://calm-tree-03352ba03
│ URL          │         .1.azurestaticapps.net
└──────────────┘
```

---

## 📊 State Management Flow

```
┌─────────────────────────────────────────────────────────┐
│                  App.tsx (Root State)                    │
│                                                           │
│  const [members, setMembers] = useState<Member[]>([]);  │
│  const [loading, setLoading] = useState(true);           │
│  const [showForm, setShowForm] = useState(false);        │
│  const [token, setToken] = useState<string>('');         │
│  const [drawerExpanded, setDrawerExpanded] = useState(true);│
│  const [selectedView, setSelectedView] = useState('members');│
│  const [formData, setFormData] = useState({...});        │
└──────────┬────────────────────────────┬─────────────────┘
           │                            │
           │ Props down                 │ Props down
           ▼                            ▼
┌──────────────────────┐    ┌─────────────────────────┐
│   AdminSidebar       │    │    MembersGrid          │
│   • expanded         │    │    • members[]          │
│   • selectedItem     │    │    • onIssueToken()     │
│   • onSelect()       │    │    • loading            │
└──────────┬───────────┘    └───────────┬─────────────┘
           │                            │
           │ Events up                  │ Events up
           │                            │
           └─────────┬──────────────────┘
                     │
                     ▼
              ┌─────────────┐
              │  App.tsx    │
              │  Handlers   │
              └─────────────┘
```

---

## 🎯 Routing Logic

```
User clicks sidebar item → handleMenuSelect(item)
                                    │
                                    ▼
                     if (item.route) { setSelectedView(item.route) }
                                    │
                     ┌──────────────┴────────────────┐
                     │                               │
                     ▼                               ▼
         selectedView === 'dashboard'    selectedView === 'members'
                     │                               │
                     ▼                               ▼
           render Dashboard View           render Members View
           ┌────────────────┐             ┌──────────────────┐
           │ Stats Cards    │             │ Action Bar       │
           │ • Total        │             │ • Register Button│
           │ • Active       │             │ MembersGrid      │
           │ • Pending      │             │ • Search         │
           │ • Premium      │             │ • Sort           │
           └────────────────┘             │ • Filter         │
                                          │ • Paginate       │
                                          └──────────────────┘
```

---

**This architecture provides:**
- ✅ Clear separation of concerns
- ✅ Unidirectional data flow
- ✅ Reusable components
- ✅ Scalable structure
- ✅ Easy to maintain and extend
